run:
	npm run dev

build:
	npm run build

serve:
	npm run serve

lint:
	npm run lint

lint-fix:
	npm run lint:fix

test:
	npm run test

test-watch:
	npm run test:watch

test-coverage:
	npm run test:coverage

clean:
	rm -rf dist && rm -rf node_modules && rm -rf coverage